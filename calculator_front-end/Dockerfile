# Використовує базовий образ для Node.js
FROM node:latest

# Встанолює робочу директорію в контейнері
WORKDIR /usr/src/app

# Копіює файли проекту
COPY . .

# Виконає необхідні кроки для збірки та запуску front-end проекту
RUN npm install

# Команда, яка виконується при старті контейнера
CMD ["npm", "start"]
